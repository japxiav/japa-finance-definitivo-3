import { BadgeCheck, FileSpreadsheet, ShieldCheck, Upload, X } from 'lucide-react';

export function ImportGuideModal({ close, continueImport }: { close: () => void; continueImport: () => void }) {
  return <div className="modal-backdrop" role="presentation"><section className="modal-card import-guide-modal" role="dialog" aria-modal="true" aria-label="Importar extrato bancário"><button type="button" className="modal-close" onClick={close}><X size={18}/></button><span className="eyebrow">IMPORTAÇÃO GUIADA</span><h2>Escolha o extrato. O app mostra tudo antes de salvar.</h2><div className="import-guide-steps"><article><FileSpreadsheet size={20}/><div><b>1. Arquivo</b><p>CSV da Wise ou Revolut. PDF continua disponível como apoio quando o extrato não oferece CSV.</p></div></article><article><BadgeCheck size={20}/><div><b>2. Prévia</b><p>Instituição, moeda, produto, período, linhas rejeitadas, pendentes e possíveis duplicatas aparecem antes da confirmação.</p></div></article><article><ShieldCheck size={20}/><div><b>3. Importação segura</b><p>Reimportar o mesmo período atualiza fatos existentes e preserva decisões manuais.</p></div></article></div><div className="import-guide-note">O arquivo é processado no navegador. A nuvem recebe o estado normalizado da sua conta, não uma pasta pública de extratos.</div><button type="button" onClick={continueImport}><Upload size={17}/> Escolher extrato</button></section></div>;
}
